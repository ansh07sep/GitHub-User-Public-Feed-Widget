Include this widget's git-activity-widget.php file on any web page to show a vertical scrolling ticker of a particular user's GitHub activity
See (or include in the page's <head> tag) git-activity-widget.js for how to initialize the ticker.

Requirements:
* jQuery

Bugs:
* Doesn't load description/message on repo creation and first master pushes
